import { createTheme } from "@mui/material";

export const theme = createTheme({
    palette: {
        mode: "dark",
        primary: {
            main: "#f9ed69",
        },
        secondary: {
            main: "#F08A5D",
        },
    },
    typography: {
        fontFamily: `roboto`, //
    },
});
